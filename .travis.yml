os: linux
dist: trusty
language: python

python:
  - "3.5"
  - "3.6"

services:
  - neo4j

addons:
  apt:
    packages:
      - libsnappy-dev

install:
  - pip install python-snappy==0.5.4
  - pip install -r requirements.txt

script:
  - make tests
